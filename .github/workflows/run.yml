name: Daily APK Domain Checker

on:
  schedule:
    - cron: '00 14 * * *' # Yeh rozana Pakistani waqt ke mutabiq sham 7 baje khud chalega
  workflow_dispatch: # Is se aap manually bhi jab chahein button daba kar chala sakte hain

jobs:
  run-script:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install requests

      - name: Run Monitor Script
        env:
          SENDER_EMAIL: ${{ secrets.SENDER_EMAIL }}
          SENDER_PASSWORD: ${{ secrets.SENDER_PASSWORD }}
          RECEIVER_EMAIL: ${{ secrets.RECEIVER_EMAIL }}
        run: python monitor.py

      - name: Upload result as backup artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: apk-domain-report
          path: apk_domains_*.txt
          retention-days: 14
